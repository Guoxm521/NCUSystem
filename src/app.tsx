// 运行时配置文件在此动态修改路由配置

export function patchRoutes({ routes }: any) {
  // console.log(routes)
  // console.log(routes[0].routes[0].routes[1].icon)
  // routes[0].routes[0].routes[1].icon = 
  // routes[0].children[0].children[1].icon= {}
  // routes.unshift({
  //   path: '/foo',
  //   exact: true,
  //   component: require('@/pages/404').default,
  // });
}